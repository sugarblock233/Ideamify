# ResearchMap backend — direct dependencies (pinned).
# requirements.lock.txt is the full transitive lock generated by `pip freeze` in a clean venv.
fastapi==0.141.1
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
pydantic==2.13.5

# managed image attachments (D 批): decode probe + pixel dimensions
Pillow==12.3.0
python-multipart==0.0.22

# test-only
httpx==0.28.1
pytest==9.1.1
